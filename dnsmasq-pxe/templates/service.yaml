apiVersion: v1
kind: Service
metadata:
  name: {{ include "dnsmasq-pxe.fullname" . }}
  labels:
    {{- include "dnsmasq-pxe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dnsmasq-pxe.selectorLabels" . | nindent 4 }}
